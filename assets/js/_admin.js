jQuery(document).ready(function($){
    $('.progress_bar').val(admin_point_level_vars.pbd_progress_bar);
    $('.rank').val(admin_point_level_vars.pbd_rank_type);
    
    $('.points').val(admin_point_level_vars.pbd_points_type);
    $('.coins').val(admin_point_level_vars.pbd_coins_type);
    $('.redeem_page').val(admin_point_level_vars.pbd_redeem_page);
 });